{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --stream",
    "start": "npm run build; lerna run start --stream"
  },
  "devDependencies": {
    "lerna": "^3.18.4"
  }
}
